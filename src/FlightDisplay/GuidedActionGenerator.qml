import QGroundControl.FlightDisplay 1.0

GuidedToolStripAction {
    text:       _guidedController.generatorTitle
    iconSource: "/res/action.svg" // provisional, change logo
    visible:    true
    enabled:    true
    actionID:   _guidedController.actionGenerator
}